import type { ResumenVentaTurnoLocal } from "../../types/ventas"

interface resumenProps{
    resumenVentas:ResumenVentaTurnoLocal|null;
}

export default function ResumenVentas({resumenVentas}:resumenProps){
    return(
        <div>

        </div>
    )
}